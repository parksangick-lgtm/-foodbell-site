# 푸드벨 (FoodBell) 작업 규칙

시제상차림·제사상차림 출장음식 **박상익출장음식**(표시 브랜드명 "푸드벨")의 소개 사이트와 견적서.
정적 HTML/CSS/JS — 빌드 도구·프레임워크 없음.

## 말과 설명

- **한국어로 말한다.** 사용자는 개발자가 아니다. 전문 용어 대신 쉬운 말로 쓰고, 꼭 필요하면 괄호로 풀어준다.
- 무엇을 왜 바꿨는지 한두 줄로 알려준다. 코드를 길게 붙여넣어 설명하지 않는다.

## 컴퓨터 세 대를 오간다

사무실 PC(`c:\gallerpark`), 노트북, 집 PC가 같은 저장소(`parksangick-lgtm/-foodbell-site`, 브랜치 **`office`**)를 공유한다.

- **작업 시작 전에 반드시 `git pull`** — 다른 컴퓨터에서 한 작업이 밀려 있을 수 있다.
- 바탕화면 아이콘이 이 일을 대신한다: **푸드벨 시작**(`시작.bat` — pull + 미리보기 서버) / **푸드벨 저장**(`저장.bat` — pull + commit + push).
- 새 컴퓨터에서는 `바탕화면-바로가기.bat` 을 더블클릭하면 두 아이콘이 생긴다.

## 미리보기

- VSCode Live Server(포트 **5500**)가 이미 떠 있는 경우가 많다. `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5500/` 로 먼저 확인한다.
- 없으면 `python -m http.server 8000`.
- 휴대폰 화면 확인은 헤드리스 크롬으로 캡처한다. **사용자가 쓰던 크롬 창을 죽이지 말 것**(`taskkill chrome` 금지) — `--user-data-dir` 을 따로 주고 새 인스턴스로 띄운다.

## 화면 만들 때

**값은 `css/style.css` 의 `:root` 에서만 고른다.** 새 색·새 크기를 즉석에서 만들지 않는다.

- 색: `--color-primary #6E1E2B` / `--color-gold #B08D57` / `--color-ink #120C06` / `--color-cream #F7F2E9` 등
- 글자 크기: `--text-xs`(12) ~ `--text-3xl`(28) + `--text-section`, `--text-hero` 램프 안에서만
- 글자 굵기: **세 단계만** — 본문 400 / 라벨·버튼 600 / 제목 700
- 간격: `--space-1`(4px) ~ `--space-24`(100px), 4px 격자
- 글꼴: 제목 `Gowun Batang`(명조), 본문 Pretendard→맑은 고딕 대체 스택

**하지 말 것**

- 아이콘에 이모지·특수문자 쓰기 → 인라인 SVG 선 아이콘으로 그린다.
- 모든 요소에 똑같은 둥근 모서리 + 그림자 두르기, 사진 위 짙은 그라데이션 + 알약 배지, `ABOUT`·`SERVICE` 같은 라틴 대문자 머리말 → "AI가 찍어낸 템플릿"으로 읽힌다. 사용자가 싫어한다.
- `box-sizing: border-box` 리셋 없이 폭 + 안쪽 여백을 함께 주기.

**휴대폰 우선** — 손님 대부분이 휴대폰으로 본다. 누르는 자리는 최소 44px, 글자는 14px 이상.

## 내용 규칙

- **가격을 사이트에 올리지 않는다.** 모든 서비스는 "상담 후 안내". 금액은 견적서에서 1:1로 정한다.
- **사이트에 없는 사실을 지어내지 않는다.** 사진 설명의 지역·날짜, 진설 방식, 구성 품목, "1:1 맞춤" 같은 약속까지 전부. 새 문구는 `index.html` 에 이미 있는 표현에서 가져오고, 없으면 사용자에게 묻는다.
- 한자를 쓸 때는 실제로 그 뜻인지 확인한다. 제례를 아는 손님이 보는 사이트다.
- 사업자 정보는 정확히: 박상익출장음식 · 대표 박상익 · 서울특별시 송파구 올림픽로34길 31 · 사업자등록번호 208-09-69358 · 010-5353-3477 · 평일 09:00-18:00.

## 사진

- 원본은 구글 드라이브(`푸드벨시제`, `푸드벨 제사`, `푸드벨행사`)에 있다.
- **이 PC에 ImageMagick 이 없다.** Python + Pillow 를 쓴다. 일괄 축소는 `python 사진용량줄이기.py`.
- **영정사진, 이름·생몰년이 보이는 묘비, 얼굴이 그대로 나온 사적인 사진은 쓰지 않는다.**

## 파일

```
index.html            메인 페이지 (홈·소개·서비스·갤러리·문의 한 장)
css/style.css         전체 스타일 + :root 토큰
js/main.js            메뉴, 탭, 라이트박스, 스크롤
sije-quote.html       시제 견적서 (편집·이미지 전송·인쇄가 이 파일 하나에)
design/               디자인 시스템 캔버스 (색·글자·간격·버튼·카드)
design/renewal/       화면 새단장 캔버스 (편집체 / 사진 화보 / 기록·문서 세 방향)
images/               실제 상차림 사진
시작.bat · 저장.bat    바탕화면 아이콘이 실행하는 작업 흐름
```

### 견적서 (`sije-quote.html`)

- 품목별 단가를 넣지 않는다. **공급가액 한 칸만 입력** → 부가세(10% 별도)와 합계가 자동 계산된다.
- "고객에게 보내기" 는 화면을 PNG 로 찍어 휴대폰 공유창(카톡·문자)으로 넘긴다. 입력값은 브라우저에 자동 저장된다.

## 배포

- 넷리파이(`astonishing-klepon-a382d5.netlify.app`)로 서비스 중이었으나 **무료 크레딧이 소진**되어 GitHub Pages 로 옮기는 중이다.
- `.github/workflows/deploy-pages.yml` 은 있지만 아직 페이지가 뜨지 않는다(404). 저장소 Settings → Pages → Source 를 **GitHub Actions** 로 두었는지 확인이 필요하다.
