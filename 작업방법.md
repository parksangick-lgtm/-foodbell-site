# 여러 컴퓨터에서 이 사이트 작업하기

이 폴더는 GitHub에 올려져 있어서 **방학동 집 PC · 사무실 PC · 처음처럼 노트북 어디서든** 최신 파일을 받아 작업하고 다시 올릴 수 있습니다.

- GitHub 저장소: <https://github.com/parksangick-lgtm/-foodbell-site>
- 공용 브랜치: **`main`** (모든 컴퓨터가 이 브랜치를 씁니다)
- ⚠️ 이 폴더는 **OneDrive 안에 두지 마세요.** `C:\dev\-foodbell-site` 처럼 OneDrive 바깥에 두어야 합니다.

---

## 가장 쉬운 방법 — 더블클릭 두 개

| 파일 | 언제 | 하는 일 |
|---|---|---|
| **시작.bat** | 작업 시작할 때 | 깃허브에서 최신 받기 → 미리보기 열기 (검은 창 닫으면 미리보기 종료) |
| **저장.bat** | 작업 끝났을 때 | 최신 받기 → 바뀐 파일 보여주기 → "무엇을 바꿨나요?" 한 줄 입력 → 깃허브에 올리기 |

Claude Code를 쓴다면 그냥 말로 해도 똑같습니다:
- 시작할 때: **"최신 내용 받아와줘"**
- 끝낼 때: **"바뀐 내용 커밋하고 올려줘"**

---

## 새 컴퓨터에서 처음 한 번만

1. **Git 설치** — <https://git-scm.com/download/win>
2. (Claude Code로 작업하면) Claude Code도 설치
3. Python 설치(선택) — <https://www.python.org/downloads/> , 첫 화면 "Add python.exe to PATH" 체크. 안 깔아도 됩니다.
4. 내 정보 등록 (검은 창에서):
   ```
   git config --global user.name "박상익"
   git config --global user.email "parksangick@gmail.com"
   git config --global credential.helper manager
   git config --global core.autocrlf true
   ```
5. 저장소 내려받기 (OneDrive 바깥 폴더에서, 예: `C:\dev`):
   ```
   git clone https://github.com/parksangick-lgtm/-foodbell-site.git
   ```
6. 처음 `저장.bat`(=push) 할 때 GitHub 로그인이 한 번 뜹니다. 이후 자동.

> 사진 원본 폴더(`E:\푸드벨시제`, `E:\푸드벨 제사`, `E:\푸드벨 행사`)는 저장소에 없습니다.
> 웹용 축소본만 `images/album/`에 들어 있습니다. 사진을 새로 고르려면 원본도 그 컴퓨터에 옮겨두세요.

---

## 명령어로 직접 하려면

```
git pull                         # ① 시작 전: 최신 받기 (항상 먼저!)
                                 # ② 작업
git add -A
git commit -m "무엇을 바꿨는지 한 줄"
git push                         # ③ 끝나면: 올리기
```

---

## 주의

- **한 컴퓨터에서 작업 → 반드시 올리기(push) → 다른 컴퓨터에서 받기(pull)** 순서를 지키세요.
- 올리기 전에는 항상 최신부터 받기(`git pull`). `저장.bat`은 자동으로 먼저 받습니다.
- 양쪽에서 같은 파일을 각각 고친 뒤 만나면 충돌이 납니다 → Claude Code에 **"충돌 해결해줘"**.
