# 사무실·노트북 양쪽에서 작업하기

이 사이트 폴더는 GitHub에 올려져 있어서, **사무실 컴퓨터와 노트북 어디서든** 최신 파일을 받아 작업하고 다시 올릴 수 있습니다.

- GitHub 저장소: <https://github.com/parksangick-lgtm/-foodbell-site>
- 사용하는 브랜치: `office` (양쪽 컴퓨터 모두 이 브랜치를 씁니다)

---

## 새 컴퓨터에서 처음 한 번만 설정

1. **Git 설치** — <https://git-scm.com/download/win> 에서 받아 설치
2. (Claude Code로 작업한다면) Claude Code도 설치
3. 내 정보 등록 (한 번만, 커밋에 이름이 남습니다):
   ```
   git config --global user.name "parksangick-lgtm"
   git config --global user.email "parksangick@gmail.com"
   git config --global credential.helper manager
   ```
4. 원하는 폴더에서 저장소 내려받기:
   ```
   git clone https://github.com/parksangick-lgtm/-foodbell-site.git
   ```
   → `-foodbell-site` 폴더가 생기고 그 안에 사이트 파일이 모두 들어옵니다.
5. 처음 `git push` 할 때 GitHub 로그인(브라우저 인증)이 한 번 뜹니다. 이후로는 자동 저장됩니다.

> 사진 원본 폴더(`E:\푸드벨시제`, `E:\푸드벨 제사`, `E:\푸드벨 행사`)는 저장소에 포함되지 않습니다.
> 웹에 올린 축소본만 `images/` 안에 들어 있습니다. 노트북에서 사진을 새로 추가하려면 그 원본도 노트북에 옮겨두어야 합니다.

---

## 매번 작업할 때

### ① 작업 시작 전 — 최신 내려받기
```
git pull
```
(다른 컴퓨터에서 바꾼 내용을 먼저 받아옵니다. **항상 먼저 하세요.**)

### ② 작업 (파일 수정 / Claude Code 사용)

브라우저에서 미리 보려면 폴더 안에서:
```
python -m http.server 8000
```
→ <http://localhost:8000> 접속. (또는 VSCode Live Server 확장, 아니면 `index.html`을 그냥 더블클릭)

### ③ 작업 끝나면 — 올리기
```
git add -A
git commit -m "무엇을 바꿨는지 한 줄"
git push
```

Claude Code를 쓴다면 그냥 이렇게 말해도 됩니다:
- 시작할 때: **"최신 내용 받아와줘"**
- 끝낼 때: **"바뀐 내용 커밋하고 올려줘"**

---

## 넷리파이(Netlify)로 웹에 올리기

GitHub에 push 한 사이트를 **무료로 자동 배포**해 주는 서비스입니다.
한 번만 연결해 두면, 이후에는 `git push`(= `저장.bat`) 할 때마다 30초~1분 뒤
실제 웹사이트가 자동으로 갱신됩니다. 서버 관리는 필요 없습니다.

### 최초 연결 (한 번만)

1. <https://app.netlify.com> 접속 → **Sign up** → **GitHub** 로 로그인
2. **Add new site → Import an existing project** 클릭
3. **Deploy with GitHub** 선택 → GitHub 권한 허용
4. 저장소 목록에서 **`-foodbell-site`** 선택
   - 안 보이면 **Configure the Netlify app on GitHub** → 저장소 접근 허용
5. 배포 설정 화면에서:
   - **Branch to deploy** : `office`
   - **Build command** : 비워둠 (`netlify.toml` 이 알아서 처리)
   - **Publish directory** : `.` (비워둬도 됨)
6. **Deploy site** 클릭 → 1분 안에 완료

> 저장소에 `netlify.toml` 설정 파일이 있어서, 게시 폴더·캐시 설정은 자동으로 잡힙니다.
> 화면에서는 **배포 브랜치를 `office` 로** 지정하는 것만 확인하면 됩니다.

### 배포 결과

- `랜덤이름-12345.netlify.app` 형태의 주소가 생깁니다.
- **Site configuration → Change site name** 에서 `foodbell` 등으로 바꾸면 → `foodbell.netlify.app`
- HTTPS(자물쇠)는 자동 적용됩니다.

### 이후 사용법 — 지금과 똑같음

`시작.bat` 으로 받고 → 수정 → `저장.bat` 으로 올리기.
push 되는 순간 넷리파이가 감지해 자동 재배포합니다.
진행 상황은 넷리파이 화면 **Deploys** 탭에서 확인("Published" 뜨면 완료).

### 사진을 바꿨는데 웹에 안 바뀔 때

브라우저나 넷리파이 CDN 캐시 때문입니다. `index.html` 에서 그 사진 주소 뒤
`?v=` 숫자를 1 올려서(예: `?v=10` → `?v=11`) 다시 올리면 됩니다.

---

## 주의

- **한 컴퓨터에서 작업 → 반드시 올리기(push) → 다른 컴퓨터에서 받기(pull)** 순서를 지키세요.
- 양쪽에서 동시에 같은 파일을 고치면 충돌이 날 수 있습니다. 이때는 Claude Code에 "충돌 해결해줘"라고 하면 됩니다.
- 올리기 전에는 항상 `git pull`부터.
